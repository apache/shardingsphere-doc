commit 6f2cf14a0da87023d7f629bf4e8ac6eac661327d
Author: Liang Zhang <zhangliang@apache.org>
Date:   Sat Sep 26 21:25:15 2026 +0800

    Clarify high-frequency invocation guidance (#39971)
    
    * Avoid duplicate unsupported SQL parser test invocations
    
    * Deduplicate Oracle ALTER PROFILE and Doris BITOR parser cases
    
    * Recover successful @Disabled test cases
    
    * Clarify high-frequency invocation guidance

diff --git a/docs/community/content/involved/conduct/code.cn.md b/docs/community/content/involved/conduct/code.cn.md
index 9cb8e9b47a6..24fc8329581 100644
--- a/docs/community/content/involved/conduct/code.cn.md
+++ b/docs/community/content/involved/conduct/code.cn.md
@@ -68,15 +68,14 @@ chapter = true
  - 创建可变集合前能够确定预计元素数量时，必须通过容量参数或接收已有集合的构造器设置足够的初始容量。
  - 当 `if`/`else` 分支分别只包含一个返回语句或对同一个变量赋值时，使用三目运算符；其他情况使用 `if`/`else`。
  - 使用 `@HighFrequencyInvocation` 标注需要重点检查性能行为的高频生产代码。
-   - 代码在以下任一情况中属于高频调用：
-     - 每次 SQL 请求都会重复执行。
-     - 每个 Pipeline 数据单元都会重复执行，包括记录、事件、数据包或批次。即使其所在方法仅调用一次或执行器仅启动一次，只要代码在内部循环中持续处理这些数据单元，仍属于高频调用。
+   - 在受支持的正常负载下，代码随吞吐量增长而重复执行时属于高频调用，包括 SQL 执行、元数据查询、事务操作或 Pipeline 数据单元处理。方法不必出现在每次 SQL 请求中；方法只调用一次但其内部重复执行的代码也属于高频调用。
+   - 对外 API 按受支持的正常逐请求用法判断，内部辅助方法沿实际调用链判断。仓库内缺少调用方、方法处于构造或关闭阶段，以及理论上可重复调用，都不能单独确定调用频率；证据不足时保留不确定结论。
    - 在能够覆盖高频行为的最小准确范围内标注类、方法或构造器。
      - 标注类时，规则适用于该类内全部方法和构造器的实现。
      - 标注方法或构造器时，规则适用于该实现及其调用的同类私有方法。
      - 即使 Lombok 生成的成员属于低频调用，只要该类中所有非 Lombok 生成的方法和构造器均属于高频调用，仍可在类上标注。
    - 仅当被标注目标是可复用的缓存资源时，才设置 `canBeCached = true`。
-   - 高频调用范围内不得执行可以预计算、缓存、复用或移到高频路径之外的高耗时操作。只有操作结果依赖当前 SQL 请求或 Pipeline 数据，并且无法在不改变正确性和生命周期的前提下移出高频路径时，才允许保留。高耗时操作包括重复 I/O、阻塞等待、反射、解析、序列化、全量遍历，以及创建大对象或大量对象。
+   - 高频调用范围内不得执行可以预计算、缓存、复用或移到高频路径之外的高耗时操作。只有操作结果依赖当前操作或数据单元，并且无法在不改变正确性和生命周期的前提下移出高频路径时，才允许保留。高耗时操作包括重复 I/O、阻塞等待、反射、解析、序列化、全量遍历，以及创建大对象或大量对象。
    - 高频调用范围内：
      - 禁止使用 Java Stream API；
      - 禁止使用 `+` 拼接字符串；
diff --git a/docs/community/content/involved/conduct/code.en.md b/docs/community/content/involved/conduct/code.en.md
index bff81cab097..b57aad7fec3 100644
--- a/docs/community/content/involved/conduct/code.en.md
+++ b/docs/community/content/involved/conduct/code.en.md
@@ -68,15 +68,14 @@ The following code of conduct is based on full compliance with the [Apache Softw
 - When the expected number of elements is known before creating a mutable collection, set a sufficient initial capacity with a capacity argument or a constructor that accepts an existing collection.
 - Use a ternary operator when each `if`/`else` branch contains only a return statement or assigns the same variable; otherwise, use `if`/`else`.
 - Use `@HighFrequencyInvocation` to mark high-frequency production code whose performance behavior requires focused review.
-    - Code is high-frequency in any of the following cases:
-        - It runs repeatedly for every SQL request.
-        - It runs repeatedly for every Pipeline data unit, including a record, event, packet or batch. Code that continuously processes those data units in an internal loop remains high-frequency even if its method is invoked only once or its executor is started only once.
+    - Code is high-frequency when its work repeats as throughput grows under a supported normal workload, including SQL execution, metadata queries, transaction operations, or Pipeline data-unit processing. A method need not run for every SQL request; repeated work inside a method invoked only once also counts.
+    - Judge external APIs by supported normal per-request use and internal helpers by actual call paths. Missing repository callers, a construction or closing role, and theoretical repeatability are not conclusive on their own; leave the result uncertain when evidence is insufficient.
     - Annotate a class, method or constructor at the smallest accurate scope that covers the high-frequency behavior.
         - On a class, the rules apply to the implementations of all methods and constructors in that class.
         - On a method or constructor, the rules apply to that implementation and the same-class private methods it calls.
         - A class annotation remains acceptable when all methods and constructors not generated by Lombok are high-frequency, even if Lombok-generated members are low-frequency.
     - Set `canBeCached = true` only when the annotated target is a cacheable resource intended for reuse.
-    - Within the high-frequency scope, do not perform expensive operations that can be precomputed, cached, reused or moved out of the high-frequency path. Retain an expensive operation only when its result depends on the current SQL request or Pipeline data and it cannot be moved without changing correctness or lifecycle. Expensive operations include repeated I/O, blocking waits, reflection, parsing, serialization, full scans, and creation of large objects or many objects.
+    - Within the high-frequency scope, do not perform expensive operations that can be precomputed, cached, reused or moved out of the high-frequency path. Retain an expensive operation only when its result depends on the current operation or data unit and it cannot be moved without changing correctness or lifecycle. Expensive operations include repeated I/O, blocking waits, reflection, parsing, serialization, full scans, and creation of large objects or many objects.
     - Within the high-frequency scope:
         - Do not use the Java Stream API;
         - Do not concatenate strings with `+`;
