commit ec40fe10eeed5b5fce4ae016af23bfad522decb5
Author: Liang Zhang <zhangliang@apache.org>
Date:   Thu Nov 6 22:29:03 2025 +0800

    Update CLAUDE.md (#37031)

diff --git a/docs/community/content/involved/conduct/code.cn.md b/docs/community/content/involved/conduct/code.cn.md
index 3528697de53..e2755de7a25 100644
--- a/docs/community/content/involved/conduct/code.cn.md
+++ b/docs/community/content/involved/conduct/code.cn.md
@@ -111,6 +111,7 @@ chapter = true
    - 模拟静态方法或构造器，应优先考虑使用测试框架提供的 `AutoMockExtension` 和 `StaticMockSettings` 自动释放资源；若使用 Mockito `mockStatic` 和 `mockConstruction` 方法，必须搭配 `try-with-resource` 或在清理方法中关闭，避免泄漏。
    - 校验仅有一次调用时，无需使用 `times(1)` 参数，使用 `verify` 的单参数方法即可。
  - 使用 `assert` 前缀命名所有的测试用例。
+ - 测试数据应使用标准化前缀（如 `foo_`/`bar_`）明确标识其测试用途。
  - 使用 `PropertiesBuilder` 简化 `Properties` 构造。
 
 ## SQL 解析规范
diff --git a/docs/community/content/involved/conduct/code.en.md b/docs/community/content/involved/conduct/code.en.md
index 1ed98722aa6..e9bc27df518 100644
--- a/docs/community/content/involved/conduct/code.en.md
+++ b/docs/community/content/involved/conduct/code.en.md
@@ -111,6 +111,7 @@ The following code of conduct is based on full compliance with the [Apache Softw
     - For mocking static methods or constructors, consider using `AutoMockExtension` and `StaticMockSettings` provided by the testing framework for automatic resource release; if using Mockito's `mockStatic` and `mockConstruction` methods, must be paired with `try-with-resource` or closed in cleanup methods to avoid leaks.
     - When verifying only one call, there's no need to use `times(1)` parameter, the single-parameter method of `verify` is sufficient.
 - Use `assert` prefix for all test method names.
+- Test data should use standardized prefixes (e.g., `foo_`/`bar_`) to clearly identify their test purpose
 - Use `PropertiesBuilder` simplify `Properties` building.
 
 ## SQL Parsing Standards
